{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "tsc",
    "build": "vite build",
    "prestart": "test -d node_modules || npm clean-install",
    "start": "npm run-script start:${NODE_ENV:-production}",
    "start:development": "vite dev",
    "start:production": "vite preview"
  },
  "dependencies": {
    "preact": "^10.5.15"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.1.5",
    "@types/node": "^16.11.43",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vite-tsconfig-paths": "^3.5.0"
  }
}
